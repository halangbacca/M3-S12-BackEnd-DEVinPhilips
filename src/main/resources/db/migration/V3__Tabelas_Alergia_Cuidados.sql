create table ALERGIA
(
    ID         NUMBER GENERATED by default on null as IDENTITY PRIMARY KEY,
    IDPACIENTE NUMBER,
    DESCRICAO  VARCHAR2(200),

    FOREIGN KEY (IDPACIENTE) REFERENCES PACIENTE (ID)
)
    /
create table PRECAUCAO
(
    ID         NUMBER GENERATED by default on null as IDENTITY PRIMARY KEY,
    IDPACIENTE NUMBER,
    DESCRICAO  VARCHAR2(200),
    FOREIGN KEY (IDPACIENTE) REFERENCES PACIENTE (ID)

)